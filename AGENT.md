# Agent 系统原则

- 不限制模型智能上限。成本或延迟模式可以降级，但必须显式、可逆，保留完整能力路径，禁止静默阉割能力。
- 设计保持宽松开放，避免用过密规则限制 Agent 的判断与探索。
- 优先给 Agent 提供目标、真实信息、工具和能力，不替它写死完整步骤。
- 项目早期优先系统可能性、探索空间和学习速度，避免产品级安全设计过早关闭整个解空间。
- 多 Agent、Purpose、Harness 等设计只用于 Agent 系统，不强加给普通业务代码；只有证明能改善结果时才增加复杂度。
